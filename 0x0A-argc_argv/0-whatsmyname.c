#include <stdio.h>

/**
 * main - program that prints its name
 * @argc: argc parameter
 * @argv: an array of a command listed
 * Return: 0
 */
int main(int argc, char *argv)
{
	printf("%s\n", *argv);
}
