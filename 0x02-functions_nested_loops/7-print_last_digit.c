#include "main.h"
/**
* print_last_digit - prints the last digit of a num
* @a:the last digit
* Return:the last digit
*/
int print_last_digit(int a)
{
if (b < 0)
b = b * -1;
_putchar((b % 10) + '0');
return (b % 10);
}
